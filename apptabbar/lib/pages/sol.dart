import "package:flutter/material.dart";

class sol extends StatefulWidget {
  const nuvem({super.key});

  @override
  State<sol> createState() => _nuvemState();
}

class _solState extends State<sol> {
  @override
  String texto = 'Aqui está uma nuvem!',

  return Column
    children[
      return "$texto";
  ],
}
